local Utils = {}

local function CalculateMinMax(startTime, rank)
    
end

local function Utils:FindMatch(matches, info)
    if matches and info then
        local rank = info.matchmaking_level
    end
end